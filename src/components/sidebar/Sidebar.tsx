import React from "react";
import classes from "./Sidebar.module.scss";

function Sidebar() {
  return <div className={`${classes.sideBar} ${classes.close}`}>Sidebar</div>;
}

export default Sidebar;
